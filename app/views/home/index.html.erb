<%= stylesheet_link_tag "application" %>


<img src="https://i.imgur.com/a4nTPrl.png" class="img-fluid">
<div>
  <h1 class= "text-center">Bienvenue à toi jeune pirate</h1>
  <h4 class= "text-center">Voici la liste complète des potins 😉</h4>
</div>
<div class="row row-cols-1 row-cols-md-3 g-4">
  <% Gossip.all.each do |gossip| %>
    <div class="col p-5 d-flex justify-content-center">
      <div class="card h-400 w-aut text-center" style="width: 40rem; border-radius: 25px; box-shadow: 1px 1px 18px 5px rgba(0, 0, 0, 0.81); background: linear-gradient(180deg, #5f490e80, #e8d39880)">
        <div class="card-body">
          <h4 class="card-title"><%= gossip.title %></h4>
          <p class="card-text">Auteur(e) : <%= gossip.user.first_name%></p>
          <%= link_to "Afficher le potin", gossip_path(gossip.id), class: "card-link link_gossip"%>
        </div>
      </div>
    </div>
  <% end %>
</div>
